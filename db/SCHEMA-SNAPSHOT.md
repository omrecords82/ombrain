# brain.db Schema Snapshot

> Auto-generated by `node om-brain/scripts/dump-schema.js`. **Do not edit manually.**
> Generated: 2026-06-28T22:05:22.572Z

Agents: **read this file at session start** for the live schema (columns, indexes, triggers, row counts).
Canonical DDL source: `om-brain/db/schema.sql` (+ migrations if not yet merged).

## Database

| Property | Value |
|----------|-------|
| Path | `/var/lib/om-brain/brain.db` |
| Tables | 20 |
| Total rows | 1762053 |

## Row counts

| Table | Rows |
|-------|------:|
| approval_requests | 10 |
| approval_status_history | 25 |
| btw_queue | 2 |
| church_memory | 1 |
| correction_memory | 1 |
| decision_memory | 36 |
| doctrine_memory | 660 |
| doctrine_vec | — |
| doctrine_vec_chunks | 0 |
| doctrine_vec_info | 4 |
| doctrine_vec_rowids | 0 |
| doctrine_vec_vector_chunks00 | 0 |
| event_memory | 1761177 |
| knowledge_memory | 3 |
| omstudio_audit | 52 |
| procedure_memory | 9 |
| system_truth_memory | 16 |
| task_memory | 2 |
| theological_memory | 26 |
| work_memory | 29 |

## approval_requests (10 rows)

### table: `approval_requests`

```sql
CREATE TABLE approval_requests (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  created_at          TEXT NOT NULL DEFAULT (datetime('now')),
  source_decision_id  INTEGER,            -- FK -> decision_memory.id
  session_id          TEXT,               -- links to work_memory.session_id
  classification      TEXT NOT NULL,      -- which human-only domain / tier0
  domains             TEXT,               -- comma-joined human-only domains
  proposal_summary    TEXT NOT NULL,      -- REDACTED summary (no secrets/tenant ids)
  state               TEXT NOT NULL,      -- current state (state machine)
  omstudio_ref        TEXT,               -- id returned by OMStudio or outbox ref
  FOREIGN KEY (source_decision_id) REFERENCES decision_memory(id)
)
```

### index: `idx_approval_decision`

```sql
CREATE INDEX idx_approval_decision ON approval_requests(source_decision_id)
```

### index: `idx_approval_state`

```sql
CREATE INDEX idx_approval_state ON approval_requests(state)
```

### trigger: `approval_requests_no_delete`

```sql
CREATE TRIGGER approval_requests_no_delete
BEFORE DELETE ON approval_requests
BEGIN
  SELECT RAISE(ABORT, 'approval_requests is non-deletable (OM-DOCTRINE-0001): DELETE forbidden');
END
```

## approval_status_history (25 rows)

### table: `approval_status_history`

```sql
CREATE TABLE approval_status_history (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  approval_id         INTEGER NOT NULL,   -- FK -> approval_requests.id
  created_at          TEXT NOT NULL DEFAULT (datetime('now')),
  from_state          TEXT,               -- null for initial creation
  to_state            TEXT NOT NULL,
  source              TEXT NOT NULL,      -- brain_submit | omstudio_ingest | dryrun_sim | create
  note                TEXT,               -- REDACTED note
  omstudio_ref        TEXT,
  FOREIGN KEY (approval_id) REFERENCES approval_requests(id)
)
```

### index: `idx_apphist_approval`

```sql
CREATE INDEX idx_apphist_approval ON approval_status_history(approval_id)
```

### trigger: `approval_status_history_no_delete`

```sql
CREATE TRIGGER approval_status_history_no_delete
BEFORE DELETE ON approval_status_history
BEGIN
  SELECT RAISE(ABORT, 'approval_status_history is append-only (OM-DOCTRINE-0001): DELETE forbidden');
END
```

### trigger: `approval_status_history_no_update`

```sql
CREATE TRIGGER approval_status_history_no_update
BEFORE UPDATE ON approval_status_history
BEGIN
  SELECT RAISE(ABORT, 'approval_status_history is append-only (OM-DOCTRINE-0001): UPDATE forbidden');
END
```

## btw_queue (2 rows)

### table: `btw_queue`

```sql
CREATE TABLE btw_queue (
  id              TEXT PRIMARY KEY,           -- UUID
  message         TEXT NOT NULL,             -- the notification text
  category        TEXT NOT NULL,             -- ops | theology | calendar | system | general
  priority        TEXT NOT NULL DEFAULT 'low', -- low | normal
  delivered       INTEGER NOT NULL DEFAULT 0, -- 0=pending, 1=delivered
  delivery_mode   TEXT NOT NULL DEFAULT 'next_interaction', -- next_interaction | scheduled
  deliver_at      TEXT,                       -- ISO-8601 if scheduled
  source_ref      TEXT,                       -- what triggered this BTW
  created_at      TEXT NOT NULL DEFAULT (datetime('now')),
  delivered_at    TEXT
, session_id TEXT, btw_id TEXT, question TEXT, mode TEXT, answer TEXT, answered INTEGER NOT NULL DEFAULT 0, answered_at TEXT)
```

### index: `idx_btw_answered`

```sql
CREATE INDEX idx_btw_answered ON btw_queue(answered)
```

### index: `idx_btw_deliver_at`

```sql
CREATE INDEX idx_btw_deliver_at ON btw_queue(deliver_at)
```

### index: `idx_btw_delivered`

```sql
CREATE INDEX idx_btw_delivered ON btw_queue(delivered)
```

### index: `idx_btw_session`

```sql
CREATE INDEX idx_btw_session ON btw_queue(session_id)
```

## church_memory (1 rows)

### table: `church_memory`

```sql
CREATE TABLE church_memory (
  id              TEXT PRIMARY KEY,           -- UUID
  place_id        TEXT,                       -- Google Places place_id (nullable for AOB-only)
  name            TEXT NOT NULL,
  jurisdiction    TEXT,                       -- e.g. "OCA", "GOARCH", "ROCOR", "Antiochian"
  address         TEXT,
  city            TEXT,
  state           TEXT,
  country         TEXT NOT NULL DEFAULT 'US',
  lat             REAL,
  lng             REAL,
  phone           TEXT,
  website         TEXT,
  liturgical_calendar TEXT,                   -- Julian | Revised Julian | Gregorian
  source          TEXT NOT NULL,              -- google_places | aob_directory | manual
  last_verified   TEXT,
  created_at      TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
, google_maps_url TEXT, rating REAL, rating_count INTEGER, canonical INTEGER, service_schedule_json TEXT, opening_hours_json TEXT, hours_source TEXT NOT NULL DEFAULT 'google_places', last_fetched_at TEXT, zip TEXT)
```

### index: `idx_church_canonical`

```sql
CREATE INDEX idx_church_canonical ON church_memory(canonical)
```

### index: `idx_church_jurisdiction`

```sql
CREATE INDEX idx_church_jurisdiction ON church_memory(jurisdiction)
```

### index: `idx_church_last_fetched`

```sql
CREATE INDEX idx_church_last_fetched ON church_memory(last_fetched_at)
```

### index: `idx_church_location`

```sql
CREATE INDEX idx_church_location ON church_memory(state, city)
```

### index: `idx_church_place_id`

```sql
CREATE INDEX idx_church_place_id ON church_memory(place_id)
```

### index: `idx_church_zip`

```sql
CREATE INDEX idx_church_zip ON church_memory(zip)
```

## correction_memory (1 rows)

### table: `correction_memory`

```sql
CREATE TABLE correction_memory (
  id                  TEXT PRIMARY KEY,       -- UUID
  decision_id         TEXT,                   -- FK -> decision_memory.id (what was wrong)
  procedure_id        TEXT,                   -- FK -> procedure_memory.id (if procedure failed)
  correction_type     TEXT NOT NULL,          -- operator_override | failed_reuse | factual_error | safety_violation
  wrong_answer        TEXT NOT NULL,          -- what the Brain said / did
  correct_answer      TEXT NOT NULL,          -- what the correct answer/action is
  explanation         TEXT,                   -- why it was wrong
  submitted_by        TEXT NOT NULL,          -- operator id or 'system'
  tags_json           TEXT,                   -- JSON array for retrieval
  embedding           BLOB,                   -- float32 vector for semantic search
  created_at          TEXT NOT NULL DEFAULT (datetime('now'))
, source_decision_id TEXT, session_id TEXT, question_type TEXT, verdict TEXT, original_output TEXT, correction TEXT, correction_source TEXT, correction_version INTEGER NOT NULL DEFAULT 1, active INTEGER NOT NULL DEFAULT 1)
```

### index: `idx_correction_active`

```sql
CREATE INDEX idx_correction_active ON correction_memory(active)
```

### index: `idx_correction_decision`

```sql
CREATE INDEX idx_correction_decision ON correction_memory(decision_id)
```

### index: `idx_correction_question_type`

```sql
CREATE INDEX idx_correction_question_type ON correction_memory(question_type)
```

### index: `idx_correction_session`

```sql
CREATE INDEX idx_correction_session ON correction_memory(session_id)
```

### index: `idx_correction_type`

```sql
CREATE INDEX idx_correction_type ON correction_memory(correction_type)
```

### trigger: `correction_memory_no_delete`

```sql
CREATE TRIGGER correction_memory_no_delete
BEFORE DELETE ON correction_memory
BEGIN
  SELECT RAISE(ABORT, 'correction_memory is append-only (OM-DOCTRINE-0001): DELETE forbidden');
END
```

### trigger: `correction_memory_no_update`

```sql
CREATE TRIGGER correction_memory_no_update
BEFORE UPDATE ON correction_memory
BEGIN
  SELECT RAISE(ABORT, 'correction_memory is append-only (OM-DOCTRINE-0001): UPDATE forbidden');
END
```

## decision_memory (36 rows)

### table: `decision_memory`

```sql
CREATE TABLE decision_memory (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id          TEXT,              -- links to work_memory.session_id
  classification      TEXT NOT NULL,     -- auto_safe_recommendation | requires_human_superadmin | tier0_halt_escalate | never_auto | informational
  recommendation      TEXT NOT NULL,     -- what the Brain proposes (NEVER executed by the Brain)
  rationale           TEXT NOT NULL,     -- why
  doctrine_rule       TEXT NOT NULL,     -- specific rule applied (deterministic engine)
  owning_system       TEXT,              -- OM | OMAI | OMStudio | cross-system
  verification_steps  TEXT,              -- how a human should verify (verification playbook)
  model_advisory      TEXT,              -- secondary LLM note (advisory only, non-authoritative)
  requires_omstudio   INTEGER NOT NULL DEFAULT 0, -- 1 = "requires human superadmin approval via OMStudio"
  created_at          TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_decision_class`

```sql
CREATE INDEX idx_decision_class ON decision_memory(classification)
```

### index: `idx_decision_session`

```sql
CREATE INDEX idx_decision_session ON decision_memory(session_id)
```

### trigger: `decision_memory_no_delete`

```sql
CREATE TRIGGER decision_memory_no_delete
BEFORE DELETE ON decision_memory
BEGIN
  SELECT RAISE(ABORT, 'decision_memory is append-only (OM-DOCTRINE-0001): DELETE forbidden');
END
```

### trigger: `decision_memory_no_update`

```sql
CREATE TRIGGER decision_memory_no_update
BEFORE UPDATE ON decision_memory
BEGIN
  SELECT RAISE(ABORT, 'decision_memory is append-only (OM-DOCTRINE-0001): UPDATE forbidden');
END
```

## doctrine_memory (660 rows)

### table: `doctrine_memory`

```sql
CREATE TABLE doctrine_memory (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  rule_key      TEXT NOT NULL,            -- e.g. "authority.human_only.schema"
  title         TEXT NOT NULL,
  body          TEXT NOT NULL,            -- the doctrine text chunk
  source_ref    TEXT NOT NULL,           -- e.g. "OM-DOCTRINE-0001 / spec §8"
  embedding     BLOB,                    -- float32 vector (nullable until embedded)
  created_at    TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_doctrine_rule_key`

```sql
CREATE INDEX idx_doctrine_rule_key ON doctrine_memory(rule_key)
```

## doctrine_vec

### table: `doctrine_vec`

```sql
CREATE VIRTUAL TABLE doctrine_vec USING vec0(embedding float[768])
```

## doctrine_vec_chunks (0 rows)

### table: `doctrine_vec_chunks`

```sql
CREATE TABLE "doctrine_vec_chunks"(chunk_id INTEGER PRIMARY KEY AUTOINCREMENT,size INTEGER NOT NULL,validity BLOB NOT NULL,rowids BLOB NOT NULL)
```

## doctrine_vec_info (4 rows)

### table: `doctrine_vec_info`

```sql
CREATE TABLE "doctrine_vec_info" (key text primary key, value any)
```

## doctrine_vec_rowids (0 rows)

### table: `doctrine_vec_rowids`

```sql
CREATE TABLE "doctrine_vec_rowids"(rowid INTEGER PRIMARY KEY AUTOINCREMENT,id,chunk_id INTEGER,chunk_offset INTEGER)
```

## doctrine_vec_vector_chunks00 (0 rows)

### table: `doctrine_vec_vector_chunks00`

```sql
CREATE TABLE "doctrine_vec_vector_chunks00"(rowid PRIMARY KEY,vectors BLOB NOT NULL)
```

## event_memory (1761177 rows)

### table: `event_memory`

```sql
CREATE TABLE event_memory (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  source        TEXT NOT NULL,           -- events | deploy_runs | log_ws | inventory
  event_type    TEXT,
  severity      TEXT,
  church_id     TEXT,                    -- nullable; redaction guards tenant exposure
  correlation   TEXT,                    -- request_id / item_id / run id when present
  payload_json  TEXT NOT NULL,           -- REDACTED payload (never-log secrets stripped)
  observed_at   TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_event_observed`

```sql
CREATE INDEX idx_event_observed ON event_memory(observed_at)
```

### index: `idx_event_type`

```sql
CREATE INDEX idx_event_type ON event_memory(event_type)
```

## knowledge_memory (3 rows)

### table: `knowledge_memory`

```sql
CREATE TABLE knowledge_memory (
  id              TEXT PRIMARY KEY,           -- UUID
  slug            TEXT NOT NULL UNIQUE,       -- stable human-readable key
  title           TEXT NOT NULL,
  body            TEXT NOT NULL,              -- the knowledge content
  category        TEXT NOT NULL,              -- platform | ops | theology | general
  tags_json       TEXT,                       -- JSON array
  source_ref      TEXT,                       -- file path, URL, or session id
  confidence      REAL NOT NULL DEFAULT 1.0,  -- 0.0–1.0
  embedding       BLOB,                       -- float32 vector (nullable)
  created_at      TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_knowledge_category`

```sql
CREATE INDEX idx_knowledge_category ON knowledge_memory(category)
```

### index: `idx_knowledge_slug`

```sql
CREATE INDEX idx_knowledge_slug ON knowledge_memory(slug)
```

## omstudio_audit (52 rows)

### table: `omstudio_audit`

```sql
CREATE TABLE omstudio_audit (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  created_at          TEXT NOT NULL DEFAULT (datetime('now')),
  kind                TEXT NOT NULL,      -- audit_event | approval_request
  source_decision_id  INTEGER,
  classification      TEXT,
  transport           TEXT NOT NULL,      -- dryrun | http
  omstudio_ref        TEXT,               -- returned id / outbox ref
  payload_json        TEXT NOT NULL       -- REDACTED payload that was transmitted
)
```

### index: `idx_omaudit_kind`

```sql
CREATE INDEX idx_omaudit_kind ON omstudio_audit(kind)
```

### trigger: `omstudio_audit_no_delete`

```sql
CREATE TRIGGER omstudio_audit_no_delete
BEFORE DELETE ON omstudio_audit
BEGIN
  SELECT RAISE(ABORT, 'omstudio_audit is append-only (OM-DOCTRINE-0001): DELETE forbidden');
END
```

### trigger: `omstudio_audit_no_update`

```sql
CREATE TRIGGER omstudio_audit_no_update
BEFORE UPDATE ON omstudio_audit
BEGIN
  SELECT RAISE(ABORT, 'omstudio_audit is append-only (OM-DOCTRINE-0001): UPDATE forbidden');
END
```

## procedure_memory (9 rows)

### table: `procedure_memory`

```sql
CREATE TABLE procedure_memory (
  id                  TEXT PRIMARY KEY,       -- UUID
  slug                TEXT NOT NULL UNIQUE,   -- stable identifier e.g. "full-system-status-check"
  title               TEXT NOT NULL,
  intent_key          TEXT NOT NULL,          -- classifier intent that triggers this procedure
  mode                TEXT NOT NULL,          -- knowledge | technical | ops
  trigger_examples    TEXT,                   -- JSON array of example phrases
  procedure_body      TEXT NOT NULL,          -- human-readable procedure description
  commands_json       TEXT,                   -- JSON array of {cmd, description, expected_output}
  required_permissions TEXT,                  -- JSON array of required permission strings
  risk_level          TEXT NOT NULL DEFAULT 'low', -- low | medium | high | destructive
  validation_steps    TEXT,                   -- JSON array of validation checks
  source_decision_id  TEXT,                   -- FK -> decision_memory.id (origin)
  source_type         TEXT,                   -- llm_extracted | operator_taught | imported
  confidence          REAL NOT NULL DEFAULT 0.0, -- 0.0–1.0
  approved            INTEGER NOT NULL DEFAULT 0, -- 0=draft, 1=approved
  approved_by         TEXT,
  approved_at         TEXT,
  usage_count         INTEGER NOT NULL DEFAULT 0,
  last_used_at        TEXT,
  created_at          TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at          TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_procedure_approved`

```sql
CREATE INDEX idx_procedure_approved ON procedure_memory(approved)
```

### index: `idx_procedure_intent`

```sql
CREATE INDEX idx_procedure_intent ON procedure_memory(intent_key)
```

### index: `idx_procedure_risk`

```sql
CREATE INDEX idx_procedure_risk ON procedure_memory(risk_level)
```

### index: `idx_procedure_slug`

```sql
CREATE INDEX idx_procedure_slug ON procedure_memory(slug)
```

## system_truth_memory (16 rows)

### table: `system_truth_memory`

```sql
CREATE TABLE system_truth_memory (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  domain        TEXT NOT NULL,           -- architecture | routing | rbac | env | action_catalog | tenant | incident
  fact_key      TEXT NOT NULL,           -- stable identifier
  body          TEXT NOT NULL,           -- human-readable fact
  source_ref    TEXT NOT NULL,           -- attachment file reference
  embedding     BLOB,
  refreshed_at  TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_systruth_key`

```sql
CREATE UNIQUE INDEX idx_systruth_key ON system_truth_memory(domain, fact_key)
```

## task_memory (2 rows)

### table: `task_memory`

```sql
CREATE TABLE task_memory (
  id              TEXT PRIMARY KEY,           -- UUID
  title           TEXT NOT NULL,
  description     TEXT,
  status          TEXT NOT NULL DEFAULT 'open', -- open | in_progress | blocked | done | cancelled
  priority        TEXT NOT NULL DEFAULT 'normal', -- low | normal | high | critical
  assigned_to     TEXT,                       -- operator or system identifier
  due_at          TEXT,                       -- ISO-8601 datetime
  tags_json       TEXT,                       -- JSON array of string tags
  source          TEXT,                       -- how it was created: manual | ingest | brain
  source_ref      TEXT,                       -- external reference (ticket id, session id, etc.)
  created_at      TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_task_priority`

```sql
CREATE INDEX idx_task_priority ON task_memory(priority)
```

### index: `idx_task_status`

```sql
CREATE INDEX idx_task_status ON task_memory(status)
```

## theological_memory (26 rows)

### table: `theological_memory`

```sql
CREATE TABLE theological_memory (
  id              TEXT PRIMARY KEY,           -- UUID
  category        TEXT NOT NULL,             -- scripture | catechism | council | patristic | liturgy | belief
  subcategory     TEXT,                       -- e.g. "OT", "NT", "Nicaea I", "Chrysostom"
  reference_key   TEXT NOT NULL,             -- e.g. "Gen.1.1", "Catechism.Q47", "Nicaea.Canon.1"
  title           TEXT,
  body            TEXT NOT NULL,             -- the text content
  source          TEXT NOT NULL,             -- e.g. "Brenton LXX 1851", "St. Philaret's Catechism"
  language        TEXT NOT NULL DEFAULT 'en',
  embedding       BLOB,
  created_at      TEXT NOT NULL DEFAULT (datetime('now'))
, source_ref TEXT, book TEXT, chapter INTEGER, verse_start INTEGER, verse_end INTEGER, topic_tags TEXT)
```

### index: `idx_theo_book_chapter`

```sql
CREATE INDEX idx_theo_book_chapter ON theological_memory(book, chapter)
```

### index: `idx_theo_category`

```sql
CREATE INDEX idx_theo_category ON theological_memory(category)
```

### index: `idx_theo_ref`

```sql
CREATE INDEX idx_theo_ref ON theological_memory(reference_key)
```

### index: `idx_theo_topic_tags`

```sql
CREATE INDEX idx_theo_topic_tags ON theological_memory(topic_tags)
```

### trigger: `theological_memory_no_delete`

```sql
CREATE TRIGGER theological_memory_no_delete
BEFORE DELETE ON theological_memory
BEGIN
  SELECT RAISE(ABORT, 'theological_memory is immutable after seeding: DELETE forbidden');
END
```

### trigger: `theological_memory_no_update`

```sql
CREATE TRIGGER theological_memory_no_update
    BEFORE UPDATE ON theological_memory
    BEGIN
      SELECT RAISE(ABORT, 'theological_memory is immutable after seeding: UPDATE forbidden');
    END
```

## work_memory (29 rows)

### table: `work_memory`

```sql
CREATE TABLE work_memory (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id      TEXT NOT NULL UNIQUE,  -- diagnostic session id
  work_item_ref   TEXT,                  -- OMAI Daily item id / change set code (CS-NNNN)
  incident_tier   TEXT,                  -- T0..T4 (see 16-brain-incident-tiers)
  state           TEXT NOT NULL,         -- open | analyzing | recommended | escalated | closed
  context_json    TEXT NOT NULL,         -- REDACTED incident/log context
  opened_at       TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### index: `idx_work_state`

```sql
CREATE INDEX idx_work_state ON work_memory(state)
```
